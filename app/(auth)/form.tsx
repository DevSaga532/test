import { View, Text } from 'react-native'
import React from 'react'
import ScreenWrapper from '@/components/ScreenWrapper'
import { Typo } from '@/components/Typo'

const Form = () => {
  return (
    <ScreenWrapper>
      <Typo size={20}>Form</Typo>
    </ScreenWrapper>
  )
}

export default Form