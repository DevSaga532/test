import { View, Text } from 'react-native'
import React from 'react'
import ScreenWrapper from '@/components/ScreenWrapper'
import { Typo } from '@/components/Typo'

const Welcomen = () => {
  return (
    <ScreenWrapper>
      <Typo size={23}>Welcomen</Typo>
    </ScreenWrapper>
  )
}

export default Welcomen